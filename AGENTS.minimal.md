# Codex Global Bootstrap Stub

`BOOTSTRAP_STUB_V1`

`AUTHORITATIVE_RULES_ROOT = ~/.meus_mcps/ai-rules`

Este arquivo deve permanecer curto. Ele existe apenas para bootstrap e redirecionamento.

1. Leia imediatamente `AUTHORITATIVE_RULES_ROOT/codex/AGENTS.authoritative.md`.
2. Se `AUTHORITATIVE_RULES_ROOT` nao existir, leia o `bootstrap.md` do repositorio publico `https://github.com/im-alexandre/meus_mcps` e execute a secao `Bootstrap de configuracoes`.
3. O bootstrap deve sobrescrever `~/.codex/AGENTS.md` com este arquivo `.minimal`, atualizar `AUTHORITATIVE_RULES_ROOT` para o path real do clone e tambem preparar `~/.claude/CLAUDE.md` com o `.minimal` correspondente.
