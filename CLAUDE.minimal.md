# Claude Global Bootstrap Stub

`BOOTSTRAP_STUB_V1`

`AUTHORITATIVE_RULES_ROOT = ~/.meus_mcps/ai-rules`

Este arquivo deve permanecer curto. Ele existe apenas para bootstrap e redirecionamento.

1. Leia imediatamente `AUTHORITATIVE_RULES_ROOT/claude/CLAUDE.authoritative.md`.
2. Se `AUTHORITATIVE_RULES_ROOT` nao existir, leia `https://github.com/im-alexandre/meus_mcps/blob/main/bootstrap.md` e execute a secao `Bootstrap de configuracoes`.
3. O bootstrap deve orientar a atualizacao de `~/.claude/CLAUDE.md` com este arquivo `.minimal`, substituindo `AUTHORITATIVE_RULES_ROOT` pelo path real do clone, e configurar `~/.claude/settings.json` e `~/.claude/mcp.json` a partir dos templates do repositorio.
