{
  "mcpServers": {
    "autodev-codebase": {
      "command": "{{AUTODEV_CMD}}",
      "args": {{AUTODEV_ARGS}},
      "env": {
        "CODEBASE_WORKSPACE": "${CLAUDE_PROJECT_DIR}"
      }
    },
    "local-llm": {
      "command": "{{PYTHON_CMD}}",
      "args": ["{{REPO_ROOT}}/server_llm.py"]
    },
    "scopus-search": {
      "command": "{{PYTHON_CMD}}",
      "args": ["{{REPO_ROOT}}/server_scopus.py"]
    },
    "docx-manager": {
      "command": "{{PYTHON_CMD}}",
      "args": ["{{REPO_ROOT}}/server_docx.py"]
    },
    "pdf-indexer": {
      "command": "{{PYTHON_CMD}}",
      "args": ["{{REPO_ROOT}}/pdf-indexer-mcp/semantic_chunked_pdf_rag.py"]
    }
  }
}
