{
  "mcpServers": {
    "local-llm": {
      "command": "{{PYTHON_CMD}}",
      "args": ["{{REPO_ROOT}}/server_llm.py"]
    },
    "scopus-search": {
      "command": "{{PYTHON_CMD}}",
      "args": ["{{REPO_ROOT}}/server_scopus.py"]
    },
    "pdf-indexer": {
      "command": "{{PYTHON_CMD}}",
      "args": ["{{REPO_ROOT}}/pdf-indexer-mcp/semantic_chunked_pdf_rag.py"]
    }
  }
}
